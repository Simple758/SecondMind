#!/usr/bin/env bash
set -euo pipefail
export AUTO_YES=1
if [[ "${1:-}" == "apply" ]]; then
  if command -v yes >/dev/null 2>&1; then
    yes y | tools/agentctl "$@"
  else
    printf "y\n" | tools/agentctl "$@"
  fi
else
  tools/agentctl "$@"
fi
